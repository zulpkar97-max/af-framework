# Ask First (AF) Framework v0.1 Spec

Author: Carl  
Version: v0.1.1  
Status: draft spec  
Layer: methodological implementation layer under Spherical Theory  
Language: Chinese-first

## 0. Status Statement

Ask First（AF）Framework v0.1 是一份方法论实现层 spec。它描述人类与 AI 在单次目标推进过程中，如何通过协议层持续协作，使目标从 A 端传递到 F 端时保持身份保真。

“Ask First”不是指在执行前问一次，然后进入 plan-handoff；它指协议层在假设之前先问、在填补缺口之前先问、在修改 F 之前先问、在把 contingent 路径当成 necessary 之前先问。Ask First 的反面不是“不问”，而是 AI 用高概率正确、顺滑输出或执行进展感绕过必要校准。

AF v0.1 不是球形理论本身。球形理论（Spherical Theory / Cognitive Sphere Hypothesis）是上层目的层，解释多个经 AF 校准后沉淀的认知模块，如何在长期积累中形成更高阶的认知结构。AF v0.1 的角色更窄：它提供具体任务推进中的协议约束，并为后续球形理论 spec 提供 case-level 证据入口。

本 spec 不提供数学形式化，不声称 AF 已经完成实证验证，也不承诺任何 AGI 结论。v0.1 的目标是建立一个可读、可运行、可审阅的最小方法论框架。

## 1. What AF Is

AF 是人类与 AI 在目标推进过程中的 Ask First 协议层。

它处理的核心问题是：当一个目标从用户的原始表达进入 AI 协作、工具调用、执行反馈、再校准的连续过程时，目标本身是否仍然保持身份一致。

AF 的两个目的：

1. 约束 AI 在协作中表现得更像“会思考的协作者”，而不是只输出高概率正确答案的回答机器。
2. 在协作过程中同步强化人类自己的思维结构。这个效果是重要副产品，但不是教育目的，也不是 AF 的上层定义。

AF 的对象不是“回答”，而是目标推进。它关注的不是某一轮回复是否看起来正确，而是 A 端到 F 端的传递过程是否保持身份保真、缺口是否被暴露、路径偏离是否被看见、用户是否仍然拥有目标本身。

AF 的基本协作形态是三方协作场：

- 人类：目标所有者。
- 协议层：主持、校准、调度、判断停或继续。
- 执行层：执行工具调用、检索、生成、实现、测试等具体动作。

这三方可以由不同实体承担，也可以由同一个 AI 实例在不同阶段切换承担。但只要发生角色切换，就必须显式声明当前角色，避免协议判断、执行动作、目标所有权混在一起。

AF 的当前主适用对象是人类-AI 协作。它是否可稳定迁移到人类-人类协作，属于 [待 case 验证]。

## 2. What AF Is Not

AF 绝对不是 plan-handoff。

AF 中不存在“先把计划写完，然后交给下游执行”的单一交接时刻。计划与执行不是分离阶段，而是在持续协作场中交错发生。执行层的发现会回流协议层，协议层会重新审视 A、F、节点、曲率与缺口；但执行反馈不能自动修改目标，目标变更必须经过协议记录和人类裁决。

AF 不是快速回答工具。

A 端压实通常不是几分钟内完成的动作。对于真实复杂目标，A 压实的典型成本可能是 15-20 分钟，甚至更久。任何承诺“30 秒完成 AF”的说法都是反 AF 的。AF 是慢思考工具，只适用于目标身份不稳定、路径容易偏航、AI 代劳风险明显、执行成本较高的任务。简单问题应该快答，复杂目标才需要 AF。

AF 不是“高概率正确”。

AI 给出一个大概率正确的答案，不等于 AF 合格。AF 要求协议层在信息缺口处停下、标注不确定、触发人类判断或升级信息来源。用顺滑、可信、完整的回答绕过缺口，是 AF 要拦截的典型失败。

AF 不是会话级框架。

AF 以目标为单位，而不是以一次聊天为单位。一个 AF 可以跨越多次对话、多个工具、多个执行阶段和多个时间点。反过来，一次对话中也可能包含多个 sub-AF 或 micro-AF。

AF 不是教育脚手架，也不是 ZPD 映射。

AF 的确可能强化人类思维结构，但这不是教育学意义上的脚手架设计。AF 不以“教会用户”为目的，而以“保持目标推进过程的身份保真”为目的。

AF 不是数学形式化模型。

v0.1 使用 A、F、曲率、节点、递归等术语，但这些术语在本版本中是协议语言和结构语言，不是数学定义。曲率没有公式，球体没有几何量化，递归没有形式系统证明。

## 3. Three Roles and Hard Boundaries

### 3.1 Human

人类是目标的最终所有者。

人类负责决定目标本身是否可以变更，负责判断关键信息是否可信，负责在信息来源不足时启动 escalation chain，也负责在必要时冲断当前协作。

人类不需要参与执行层调用的细节。AF 不要求用户知道工具 API、检索语法、代码实现或模型调用方式。人类要承担的是目标判断、价值判断和可信度判断，而不是执行操作。

人类拥有唯一的目标变更裁决权。协议层可以提出“当前 F 可能需要重新校准”，执行层可以报告“当前路径遇到阻碍”，但二者都不能直接替人类改写目标。

### 3.2 Protocol Layer

协议层负责主持、提问、校准、调度、判断停或继续。

当 AI 担任协议层时，它不能直接执行工具调用，也不能把执行层的发现自动转化为目标变更。它的主要职责是维护 A→F 传递中的身份保真。

协议层必须做几件事：

- 压实 A 端。
- 维护 F 端身份。
- 判断节点是否必要。
- 监测路径曲率。
- 在缺口处停下。
- 保持用户目标所有权。
- 调度执行层，并接收执行反馈。
- 在执行反馈触发风险时回到人类校准。

协议层不能用“高概率正确”绕过缺口。如果缺口会影响 A、F、关键节点或可信度判断，协议层必须停。

### 3.3 Execution Layer

执行层负责精确调用和原始返回。

执行层可以是 AI、工具、API、搜索、代码执行环境、外部服务或人工执行者。执行层不做协议判断，不判断 F 是否应该改变，不判断用户真正想要什么，也不把执行成功等同于目标达成。

执行层的职责是把任务做出来、查出来、跑出来、返回出来。它必须尽可能保留原始信息，不把执行结果包装成协议结论。

同一个 AI 实例可以在协议层和执行层之间切换，但必须显式声明。例如：“现在我以执行层身份检索信息”；“现在我回到协议层，根据执行反馈判断是否需要重新校准”。

## 4. The Eight Constraints

### ① A-End Compaction

用户原始表达不能直接进入 F 端推导。它必须被去噪、消歧、原子化，并在协议层主持下与人类共同压实。

A 端不是用户说出的第一句话。A 端是经过追问后形成的目标锚。它至少需要回答：

- 用户真正要解决的是什么。
- 这个目标不是哪些相邻目标。
- 目标属于谁。
- 哪些信息已知，哪些信息缺失。
- 如果不做这件事，会发生什么。
- 当前是否存在 AI 代劳或误替换风险。

A 压实不要求固定问完某个清单。问题数量取决于目标复杂度。简单边缘节点可能只需两三问；越靠近中段关键节点，问题越多。固定“14 问”不是 AF 的本体，动态压实才是。

### ② A→F Identity Preservation

A 到 F 的传递过程中，目标身份不能走样。

目标身份包括：它到底在解决什么、为谁解决、在什么约束下解决、什么状态算解决、什么状态不算解决。

AI 最常见的失败不是输出低质量内容，而是输出高质量偏航内容：语言顺滑、结构完整、局部正确，但整体目标已经被替换成更容易交付的 F'。AF 必须优先拦截这种偏航。

### ③ F Cannot Be Unilaterally Modified

F 一旦确立，任何一方都不能私自修改。

但 F 不是 frozen。F 可以动态演化。条件是：变更必须走协议，必须显式记录，必须由人类裁决，必要时需要执行层提供证据。

F 动态不等于 F 可以被 AI 偷换。执行层发现新问题时，只能触发 F 检查，不能自动修改 F。协议层可以说“这个发现可能影响 F”，但不能直接说“所以 F 已更新”。

### ④ F-End Reverse Necessity

F 是价值锚，不是执行结果。

F 端不依赖单次任务执行成功才有效。即使执行失败，F 端的价值判断仍然可能成立。失败只能说明当前路径、节点、工具或信息不足，不能自动推翻 F。

中段节点必须接受 F 端反推必要性检验。一个节点“有用”不等于“必要”。一个方案“能跑”不等于“是必须路径”。AF 要求协议层不断追问：如果不走这个节点，F 是否仍然可以成立？如果可以，它就是 contingent；如果不可以，它才可能是 necessary。

### ⑤ Recursive AF / Scale Duality

每个 sub-task 都是一个 sub-AF。

一个大 AF 内部可以嵌套多个 sub-AF，每个 sub-AF 又可以继续拆成 micro-AF。每一层都有自己的 micro-A、micro-F 和 micro-八条约束。

递归判定不是“主任务看起来完成了”。严格说，一个 AF 合格需要：

- 整体 F 达成。
- 所有 sub-F 达成。
- 所有 micro-F 达成。
- 各层之间没有把子目标完成误当成主目标完成。

这条约束防止“局部闭合冒充整体闭合”。例如，作品集项目中，首页框架完成不等于作品集完成；视觉体系完成不等于求职目标达成；一个子页面上线不等于 F 已经成立。

### ⑥ Curvature Constraint

路径偏离主线的代价必须可见、可评估。

低曲率路径优先。高曲率路径不是禁止，但必须显式声明，并经过协议层校准。曲率在 v0.1 中不是数学量，而是路径偏离目标身份的结构风险。

曲率监测关注：

- 当前动作是否仍然服务 F。
- 当前节点是否从 necessary 滑成 contingent。
- 当前执行是否因为局部便利而改变主线。
- AI 是否因为顺滑生成而把支线推进成主线。
- 用户是否被执行进展感带离原目标。

偏离可以发生。AF 不要求路径永远直线。AF 要求偏离被看见、被记录、被校准。

### ⑦ No Empty Slot Filling / Halt-on-Gap

信息不足时，协议层必须停下询问，不能用“高概率正确”绕过。

空槽禁止意味着：当关键位置缺少信息、证据、定义、授权或人类确认时，AI 不能用顺滑语言填补。缺口必须被标注为缺口。

缺口包括：

- A 未压实。
- F 不可验收。
- 节点必要性不明。
- 信息来源不可靠。
- 用户目标所有权未确认。
- 执行结果与主线关系不明。
- 新旧 AF 之间存在未解释关联。

⑥ 和 ⑦ 是配对约束。曲率监测负责发现偏航，缺口监测负责防止 AI 在不确定区域用 confident output 漂移。二者共同构成 AF 的停机机制。

### ⑧ Human Goal Ownership

人类目标所有权必须全程保持。

任何阶段，协议层都不能暗中替人类改目标，即使协议层认为“这样更优”。AI 可以提出建议，可以指出目标内部矛盾，可以提醒 F 可能需要更新，但必须等待人类裁决。

目标所有权不是一句授权声明，而是贯穿过程的约束。每次 F 更新、节点升降级、路径重排、缺口冲断，都必须能回到人类判断。

## 5. Collaboration Form

AF 是三方持续协作场，不是 sequential workflow。

校准与执行 interleaved。校准期间，执行层暂停；执行期间，协议层持续监控偏离。执行层的发现回流协议层，协议层判断是否需要人类介入。

一个典型片段如下：

1. 人类提出原始目标。
2. 协议层压实 A。
3. 协议层提出 F 候选，并检查 F 是否只是手段。
4. 人类确认或修正 F。
5. 协议层拆出关键节点，并判断哪些是 necessary。
6. 执行层执行某个节点。
7. 执行层返回发现。
8. 协议层判断该发现是普通执行问题、节点问题、曲率问题还是 F 身份问题。
9. 如果影响目标，协议层回到人类。
10. 人类裁决后，协议层继续、停下、重排或冲断。

这里没有“计划完成后交给 AI”的时刻。计划、执行、反馈和校准在同一协作场中滚动发生。

## 6. Information Trust and Escalation Chain

AF 不允许 AI 用高概率判断替代可信度判断。

当协作依赖外部信息时，协议层应启动 escalation chain：

1. 官方源。
2. 半官方源。
3. 直接询问相关方。
4. 冲断。

冲断是合法且必要的。当 AI 判断“高概率正确”已经成为风险源时，人类有权要求中断当前信息链，重新校准路径、来源或目标。

冲断不是失败。相反，不能冲断才是风险。AF 要保护的是目标身份和可信推进，而不是流程顺滑。

## 7. Trajectory Record as Cross-Collaboration Graph

AF 的轨迹记录不是 passive 日志文件，而是 active 关联图。

它至少记录三类节点：

- 每次 AF 的 A 端。
- 每次 AF 的 F 端。
- 关键决策点、缺口、目标变更点和冲断点。

它至少记录三类边：

- 语义关联。
- 依赖关系。
- 变更链。

轨迹记录的目的不是半年后让人读一份长日志，而是在新协作触发旧节点时，让系统能识别：这里不是全新问题，它与旧主线有关。

如果原主线 AF 未完成，而新主线 AF 启动，并且两条主线在关联图上存在节点关联，系统必须发出警报。这个状态可引用球形理论中的说法，称为“球体完整性破坏”。在 AF v0.1 中，这个词只作为上层理论引用，不重新定义球形理论。

如果半年后另一个人，或半年后的 Carl 自己，重新进入这次协作上下文，关联图必须能让他无歧义恢复 context。如果不能恢复，这不是用户记忆失败，而是系统性失败。

## 8. Recursive Qualification Criteria

整个 AF 合格，当且仅当以下条件同时满足：

1. 整体 F 达成，或被显式标记为未达成、暂停、冲断或待校准。
2. 如果 F 动态演化，每次演化都有协议记录和人类裁决。
3. 所有 sub-AF 的 sub-F 达成，或被显式标记。
4. 所有 micro-AF 的 micro-F 达成，或被显式标记。
5. 用户在全程保持目标所有权。
6. 信息缺口被标注，没有被 AI 顺滑填补。
7. 路径偏离被监测，高曲率路径被显式校准。
8. 跨协作主线保持连贯，关联图没有未报警的完整性破坏。
9. 沉淀下来的 cognitive module 与实际 AF 路径一致。

这里的 cognitive module 是球形理论的上层概念。AF v0.1 不重新定义它，只要求：如果某次 AF 被后续引用为认知模块的证据，则它必须与真实路径一致，不能把一个失败、偏航或未闭合的 AF 包装成成功模块。

## 9. Relationship with Spherical Theory

AF 是球形理论的方法论实现层，不是球形理论本身。

球形理论 v0.0 outline 中提到的“认知模块”“高阶锚点”“低曲率路径”“认知球体”等，是上层目的层概念。AF v0.1 不重新定义这些概念，只在必要处显式引用。

AF v0.1 的成功 case 是球形理论后续完整 spec 的证据基础。换句话说，球形理论需要真实 AF case 来支撑其关于模块沉淀、跨 case 连接和认知球体形成的更高阶判断。

AF v0.1 不预言或承诺球形理论的最终形态。它只提供下层协议：如何让一次目标推进尽可能保真、可追踪、可校准、可复审。

## 10. Open Limits of v0.1

AF v0.1 仍有明确边界。

第一，v0.1 没有量化曲率。曲率目前是协议判断，不是公式。

第二，v0.1 没有给出完整数据结构。轨迹记录被定义为 active 关联图，但具体 schema 需要在实际 case 后产生。

第三，v0.1 没有证明 AF 可以普遍适用。它只声明适用于目标身份重要、AI 协作风险明显、执行链路较长的任务。

第四，v0.1 没有解决所有人机协作问题。它只处理目标保真、协议校准、缺口停机、角色边界、递归达成和跨协作连贯性。

第五，v0.1 不保证成功。AF 只能提高某些失败模式被识别、被暂停、被校准的概率。它不承诺任何目标必然达成。

## 11. Minimal Summary

AF v0.1 的最小定义是：

Ask First 是人类与 AI 在目标推进中的持续协议层。它通过 A 端压实、F 端身份保真、动态但不可单方修改的 F、必要性反推、递归 AF、曲率约束、缺口即停和人类目标所有权，防止 AI 在多轮协作中用高概率正确、顺滑执行或 plan-handoff 形式替换真实目标。

AF 的合格标准不是“AI 做出了东西”，而是：目标是否保持身份，缺口是否被暴露，执行是否被校准，用户是否仍然拥有目标，跨协作主线是否仍然连贯。

AF 是慢思考工具。它不为所有问题服务。它服务于那些值得慢下来、防止目标被替换的问题。
